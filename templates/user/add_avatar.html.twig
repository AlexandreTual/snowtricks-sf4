{% extends 'base.html.twig' %}

{% block title %}{{ 'page.add.avatar.name'|trans }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-3">&nbsp;</div>
            <div class="col-md-6">
                <div class=" py-3 px-3">
                    <h1 class="mb-4">{{ 'page.add.avatar.title'|trans }}</h1>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button type="submit" class="btn btn-primary">{{ 'word.save'|trans }}</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}