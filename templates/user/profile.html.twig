{% extends 'base.html.twig' %}

{% block title %}Edition du profile{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edition du profile de {{ app.user.fullname }}</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success mb-5">Enregistrer les modifications</button>
        {{ form_end(form) }}
    </div>
{% endblock %}