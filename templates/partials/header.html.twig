<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">Snowtrick</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_home_index') }}">Accueil</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-link dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        <img src="{{ app.user.picture }}" alt="Avatar de {{ app.user.fullname }}" class="avatar avatar-mini">  {{ app.user.fullname }}
                    </a>

                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                    <a href="{{ path('app_user_show') }}" class="dropdown-item">Profil</a>
                    <div class="dropdown-divider"></div>
                    <a href="{{ path('app_security_logout') }}" class="dropdown-item">Deconnexion</a>
                </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a href="{{ path('app_security_registration') }}" class="nav-link">Inscription</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_security_login') }}" class="nav-link">Connexion</a>
                </li>
            {% endif %}
        </ul>
    </div>

</nav>