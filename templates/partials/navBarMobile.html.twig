<nav class="navbar-dark bg-secondary fixed-bottom" id="navBarMobile">
    <div class="col-4 d-flex align-items-center justify-content-center">
        <a class="navbar-brand" href="{{ path('app_app_index') }}">
            <i class="fas fa-home"></i>
        </a>
    </div>
    <div class="col-4 d-flex align-items-center justify-content-center">
        <ul class="navbar-nav">
            <li class="nav-item dropdown active">
                <a href="#" class="nav-link" data-toggle="dropdown" id="categoryDropdownLink" ><i class="fas fa-bookmark"></i></a>
                <div class="dropdown-menu" aria-labelledby="categoryDropdownLink">
                    {{ render(controller('App\\Controller\\CategoryController::categoriesList')) }}
                </div>
            </li>
        </ul>
    </div>
    <div class="col-4 d-flex align-items-center justify-content-center">
        <ul class="navbar-nav">
            {% if app.user %}
                <li class="nav-link dropdown">
                    <a href="#" class="nav-link" data-toggle="dropdown" id="accountDropdownLink">
                        <img src="{{ app.user.picturePath }}" alt="{{ 'word.avatar.to'|trans }} {{ app.user.fullname }}" class="avatar avatar-mini">
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('app_admin_index') }}" class="dropdown-item">{{ 'word.admin.space'|trans }}</a>
                            <div class="dropdown-divider"></div>
                        {% endif %}
                        <a href="{{ path('app_user_profile', {'slug': app.user.slug}) }}" class="dropdown-item">{{ 'word.profile'|trans|capitalize }}</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('app_trick_add') }}" class="dropdown-item">{{ 'word.add.trick'|trans|capitalize }}</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('app_security_logout') }}" class="dropdown-item">{{ 'word.logout'|trans|capitalize }}</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a href="{{ path('app_security_login') }}" class="nav-link"><i class="fas fa-sign-in-alt"></i></a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>